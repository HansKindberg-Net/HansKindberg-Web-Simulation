@if(this.Request.IsAuthenticated)
{
    <text>Welcome <b>@this.Html.ActionLink(this.Context.User.Identity.Name, "ChangePassword", "Account")</b>! / @this.Html.ActionLink("Log off", "LogOff", "Account")</text>
}
else
{
    <text>@this.Html.ActionLink("Log on", "LogOn", "Account")</text>
}